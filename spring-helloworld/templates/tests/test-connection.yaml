apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-ex.fullname" . }}-test-connection"
  labels:
    {{- include "spring-ex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-ex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
